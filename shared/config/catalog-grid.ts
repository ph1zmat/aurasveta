/**
 * Стандартные классы сетки карточек товаров для всех страниц storefront.
 *
 * На мобайле всегда 2 колонки; на больших экранах расширяется в зависимости
 * от контекста страницы. Менять здесь — менять везде.
 */
export const CATALOG_GRID = {
	/** 2-кол → 3-кол на lg. Для страниц без боковой панели (избранное). */
	default: 'grid grid-cols-2 gap-3 lg:grid-cols-3',
	/** 2-кол → 4-кол на lg. Для секций категорий на главной странице каталога. */
	wide: 'grid grid-cols-2 gap-3 lg:grid-cols-4',
	/** 2-кол → 3-кол на xl. Для страниц с боковой панелью фильтров. */
	sidebar: 'grid grid-cols-2 gap-3 xl:grid-cols-3',
} as const
